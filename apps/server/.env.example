PORT=3001
WEB_ORIGIN=http://localhost:5173

# Clerk — secret key server-only (never commit real values)
CLERK_SECRET_KEY=

# Supabase — service role bypasses RLS; keep on the server only
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
