// =============================================================================
// Deprecated — superseded by `components/ui/AppPillNav`.
// -----------------------------------------------------------------------------
// The per-page nav components were consolidated into a single AppPillNav so
// the link cluster, mobile rules, and chrome stay in sync across signed-in
// pages. Nothing in the app imports this module any more.
// =============================================================================

export {};
